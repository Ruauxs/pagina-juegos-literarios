// Función para generar el código QR function generarQR() { const qrCodeContainer = document.getElementById('qr-code'); // URL que queremos que el código QR apunte (puedes cambiarla) const qrUrl = 'https://www.example.com'; // Cambia a la URL deseada const qrCode = new QRCode(qrCodeContainer, { text: qrUrl, width: 128, height: 128, colorDark: '#000000', colorLight: '#ffffff', correctLevel: QRCode.CorrectLevel.H }); } // Llamar a la función para generar el QR cuando se cargue la página window.onload = function() { generarQR(); };
